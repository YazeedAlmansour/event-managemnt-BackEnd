--CREATE TABLE USERS(
--    USER_NUMBER BIGINT NOT NULL AUTO_INCREMENT ,
--    USER_ID INT PRIMARY KEY,
--    USER_FNAME VARCHAR(50) NOT NULL,
--    USER_LNAME VARCHAR(50) NOT NULL,
--    USER_EMAIL VARCHAR(320) NOT NULL,
--    USER_PHONE VARCHAR(10) NOT NULL,
--    USER_GENDER VARCHAR(6) NOT NULL,
--    USER_BIRTH DATE NOT NULL,
--    USER_PASS VARCHAR(20) NOT NULL,
--    USER_ROLE_ID VARCHAR(10),
--    USER_ISDELETED BIT
--  );
--
--CREATE TABLE ROLE(
--    ROLE_ID INT PRIMARY KEY,
--    ROLE_NAME VARCHAR(50) NOT NULL,
--    ROLE_ISDELETED BIT
--);
--
--CREATE TABLE EVENT(
--    EVENT_NUMBER BIGINT PRIMARY KEY AUTO_INCREMENT,
--    EVENT_NAME VARCHAR(300) NOT NULL,
--    EVENT_LOCATION VARCHAR(300) NOT NULL,
--    EVENT_CAPACITY VARCHAR(10) NOT NULL,
--    EVENT_DATE_TIME TIMESTAMP NOT NULL,
--    EVENT_APPROVAL BIT NOT NULL,
--    EVENT_ORGNAIZER_ID VARCHAR(10),
--    EVENT_ISDELETED BIT
--);
--
--
--CREATE TABLE TICKET(
--    TICKET_NUMBER BIGINT PRIMARY KEY AUTO_INCREMENT,
--    TICKET_CONFORMED BIT NOT NULL,
--    TICKET_ATTENDER_ID VARCHAR(10),
--    TICKET_EVENT_NUMBER BIGINT,
--    TICKET_ISDELETED BIT
--);
--
--
--CREATE TABLE REVIEW(
--    REVIEW_NUMBER BIGINT PRIMARY KEY AUTO_INCREMENT,
--    REVIEW_COMMENT VARCHAR(300),
--    REVIEW_RATE INT NOT NULL,
--    REVIEW_ATTENDER_ID VARCHAR(10),
--    REVIEW_EVENT_NUMBER BIGINT,
--    REVIEW_ISDELETED BIT
--);
--
--ALTER TABLE USERS ADD FOREIGN KEY (USER_ROLE_ID) REFERENCES ROLE(ROLE_ID);
--
--ALTER TABLE EVENT ADD FOREIGN KEY (EVENT_ORGNAIZER_ID) REFERENCES USERS(USER_ID);
--
--ALTER TABLE TICKET ADD FOREIGN KEY (TICKET_ATTENDER_ID) REFERENCES USERS(USER_ID);
--ALTER TABLE TICKET ADD FOREIGN KEY (TICKET_EVENT_NUMBER) REFERENCES EVENT(EVENT_NUMBER);
--
--ALTER TABLE REVIEW ADD FOREIGN KEY (REVIEW_ATTENDER_ID) REFERENCES USERS(USER_ID);
--ALTER TABLE REVIEW ADD FOREIGN KEY (REVIEW_EVENT_NUMBER) REFERENCES EVENT(EVENT_NUMBER);
--
--
